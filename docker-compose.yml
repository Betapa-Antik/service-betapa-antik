version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: betapa-antik-service
    ports:
      - "${PORT:-8081}:${PORT:-8081}"
    env_file:
      - .env
    restart: unless-stopped
